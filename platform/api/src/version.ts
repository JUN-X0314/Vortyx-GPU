// The API layer's version (Phase 15) — ONE source for the software_version
// the API reports. Mirrors src/core/version.hpp (the C++ side's single
// source); both sides are bumped together and the version tests + docs
// pin the agreement.

export const SOFTWARE_VERSION = "0.16.0";
